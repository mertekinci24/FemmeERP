<UserControl x:Class="InventoryERP.Presentation.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="Transparent">
    <StackPanel Margin="8">
      <TextBlock Text="Raporlar" FontSize="24" />
      <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
        <Button Content="Yedekle..." Width="120" Command="{Binding BackupCmd}" Margin="0,0,8,0" />
        <Button Content="Geri YÃ¼kle..." Width="120" Command="{Binding RestoreCmd}" Margin="0,0,8,0" />
        <Button Content="Stok DeÄŸer Raporu" Width="150" Command="{Binding StockValuationReportCmd}" />
      </StackPanel>
    </StackPanel>
  </Grid>
</UserControl>
