<Window x:Class="InventoryERP.Presentation.Views.DbDiagnosticsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VeritabanÄ± TeÅŸhis" Height="420" Width="680">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Text="VeritabanÄ± TeÅŸhis (Dinamik)" FontSize="20" FontWeight="SemiBold"/>

    <StackPanel Grid.Row="1" Margin="0,12,0,12">
  <TextBlock Text="BaÄŸlantÄ± Dizgesi:" FontWeight="SemiBold"/>
  <TextBox Text="{Binding ConnectionString, Mode=OneWay}" IsReadOnly="True" Margin="0,4,0,8"/>

  <TextBlock Text="VeritabanÄ± DosyasÄ±:" FontWeight="SemiBold"/>
  <TextBox Text="{Binding DatabasePath, Mode=OneWay}" IsReadOnly="True" Margin="0,4,0,8"/>

  <TextBlock Text="Durum:" FontWeight="SemiBold"/>
  <TextBox Text="{Binding Status, Mode=OneWay}" IsReadOnly="True" Margin="0,4,0,8"/>

      <TextBlock Text="Uygulanan Migration'lar:" FontWeight="SemiBold"/>
      <ListBox ItemsSource="{Binding AppliedMigrations}" Height="80"/>

      <TextBlock Text="Bekleyen Migration'lar:" FontWeight="SemiBold" Margin="0,8,0,0"/>
      <ListBox ItemsSource="{Binding PendingMigrations}" Height="80"/>

      <TextBlock Text="Derlenen (Assembly) Migration'lar:" FontWeight="SemiBold" Margin="0,8,0,0"/>
      <ListBox ItemsSource="{Binding DiscoveredMigrations}" Height="80"/>

      <TextBlock Text="Partner Tablo SÃ¼tunlarÄ±:" FontWeight="SemiBold" Margin="0,8,0,0"/>
      <ListBox ItemsSource="{Binding PartnerColumns}" Height="80"/>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Migration Uygula" Command="{Binding ApplyMigrationsCmd}" Margin="0,0,8,0"/>
      <Button Content="KlasÃ¶rÃ¼ AÃ§" Command="{Binding OpenFolderCmd}" Margin="0,0,8,0"/>
      <Button Content="Kapat" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>