<UserControl x:Class="InventoryERP.Presentation.Views.SalesModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- R-060: SatÄ±ÅŸ ve Pazarlama: Teklifler, SipariÅŸler -->
  <DockPanel>
    <!-- Toolbar for Sales-specific actions -->
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8,8,8,8">
      <Button Content="ğŸ“‹ Yeni Teklif" Command="{Binding NewQuoteCmd}" Width="160" Height="32" Margin="0,0,8,0"/>
      <Button Content="ğŸ“¦ Yeni SatÄ±ÅŸ SipariÅŸi" Command="{Binding NewSalesOrderCmd}" Width="180" Height="32" Margin="0,0,8,0"/>
    </StackPanel>
    <TabControl>
      <TabItem Header="ğŸ’¼ Teklifler">
        <ContentControl Content="{Binding QuotesView}"/>
      </TabItem>
      <TabItem Header="ğŸ“¦ SatÄ±ÅŸ SipariÅŸleri">
        <ContentControl Content="{Binding SalesOrdersView}"/>
      </TabItem>
    </TabControl>
  </DockPanel>
</UserControl>
