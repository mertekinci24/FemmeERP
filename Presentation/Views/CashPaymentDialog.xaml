<Window x:Class="InventoryERP.Presentation.Views.CashPaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:InventoryERP.Presentation.ViewModels.Cash"
        Title="Ödeme Fişi" 
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Cash Account -->
        <Label Grid.Row="0" Grid.Column="0" Content="Kasa/Banka:"/>
        <ComboBox Grid.Row="0" Grid.Column="1"
                  ItemsSource="{Binding CashAccounts}"
                  SelectedItem="{Binding SelectedCashAccount}"
                  DisplayMemberPath="Name"/>

        <!-- Partner -->
        <Label Grid.Row="1" Grid.Column="0" Content="Cari (Opsiyonel):"/>
        <ComboBox Grid.Row="1" Grid.Column="1"
                  ItemsSource="{Binding Partners}"
                  SelectedItem="{Binding SelectedPartner}"
                  DisplayMemberPath="Name"
                  IsEditable="True"/>

        <!-- Date -->
        <Label Grid.Row="2" Grid.Column="0" Content="Tarih:"/>
        <DatePicker Grid.Row="2" Grid.Column="1"
                    SelectedDate="{Binding Date}"/>

        <!-- Amount -->
        <Label Grid.Row="3" Grid.Column="0" Content="Tutar:"/>
        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Description -->
        <Label Grid.Row="4" Grid.Column="0" Content="Açıklama:"/>
        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding Description}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Height="80"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Margin="5"
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Kaydet" Command="{Binding SaveCommand}" IsDefault="True"/>
            <Button Content="İptal" Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
