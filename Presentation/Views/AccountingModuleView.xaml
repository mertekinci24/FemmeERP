<UserControl x:Class="InventoryERP.Presentation.Views.AccountingModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- R-044.1: Accounting Module - Invoices and Partners (Cari) -->
  <DockPanel>
    <TabControl>
      <TabItem Header="ğŸ§¾ Belgeler (Faturalar/FiÅŸler)">
        <ContentControl Content="{Binding DocumentsView}"/>
      </TabItem>
      <TabItem Header="ğŸ‘¥ Cari Hesap KartlarÄ±">
        <ContentControl Content="{Binding PartnersView}"/>
      </TabItem>
    </TabControl>
  </DockPanel>
</UserControl>
