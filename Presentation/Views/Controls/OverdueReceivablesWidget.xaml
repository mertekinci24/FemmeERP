<UserControl x:Class="InventoryERP.Presentation.Views.Controls.OverdueReceivablesWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="140" d:DesignWidth="320">
    <Border Padding="8" CornerRadius="6" BorderBrush="#DDD" BorderThickness="1" Background="White">
        <StackPanel>
            <TextBlock Text="Vadesi GeÃ§miÅŸ Alacaklar" FontWeight="Bold" FontSize="14"/>
            <TextBlock x:Name="SummaryText" Text="0.00 TRY â€” 0 partners" Margin="0,4,0,8"/>
            <ItemsControl x:Name="TopList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="{Binding Name}" Width="160"/>
                            <TextBlock Text="{Binding TotalOverdue, StringFormat=C2}" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
