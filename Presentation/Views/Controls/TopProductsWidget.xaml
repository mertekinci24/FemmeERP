<UserControl x:Class="InventoryERP.Presentation.Views.Controls.TopProductsWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Border Padding="8" CornerRadius="6" BorderBrush="#DDD" BorderThickness="1" Background="White">
        <StackPanel>
            <TextBlock Text="En Ã‡ok SatÄ±lan ÃœrÃ¼nler" FontWeight="Bold" FontSize="14"/>
            <ItemsControl x:Name="TopList" Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="80" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Sku}" Grid.Column="0" />
                            <TextBlock Text="{Binding Quantity}" Grid.Column="1" HorizontalAlignment="Right" />
                            <TextBlock Text="{Binding RevenueTry, StringFormat=C2}" Grid.Column="2" HorizontalAlignment="Right" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
