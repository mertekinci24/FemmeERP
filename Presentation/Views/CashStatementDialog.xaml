<Window x:Class="InventoryERP.Presentation.Views.CashStatementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:InventoryERP.Presentation.ViewModels.Cash"
        mc:Ignorable="d"
        Title="{Binding AccountName, StringFormat='Kasa Ekstresi - {0}'}" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filters -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Başlangıç:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker SelectedDate="{Binding FromDate}" Width="120"/>
            
            <TextBlock Text="Bitiş:" VerticalAlignment="Center" Margin="15,0,5,0"/>
            <DatePicker SelectedDate="{Binding ToDate}" Width="120"/>
            
            <Button Content="Yenile" Command="{Binding RefreshCmd}" Margin="15,0,0,0" Padding="10,2"/>
        </StackPanel>

        <!-- Ledger Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tarih" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="Auto"/>
                <DataGridTextColumn Header="Belge No" Binding="{Binding DocNumber}" Width="Auto"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="*"/>
                
                <DataGridTextColumn Header="Giriş (Borç)" Binding="{Binding Debit, StringFormat=N2}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Green"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Debit}" Value="0">
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="Opacity" Value="0.5"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Çıkış (Alacak)" Binding="{Binding Credit, StringFormat=N2}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Red"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Credit}" Value="0">
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="Opacity" Value="0.5"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Bakiye" Binding="{Binding Balance, StringFormat=N2}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="10" Margin="0,10,0,0" CornerRadius="4">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Toplam Giriş:" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TotalIn, StringFormat=N2}" Foreground="Green" Margin="0,0,20,0"/>

                <TextBlock Text="Toplam Çıkış:" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TotalOut, StringFormat=N2}" Foreground="Red" Margin="0,0,20,0"/>

                <TextBlock Text="Son Bakiye:" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding EndingBalance, StringFormat=N2}" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
