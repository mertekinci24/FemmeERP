<UserControl x:Class="InventoryERP.Presentation.Views.StockModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- R-045: Stock & Warehouse Module with Tabs -->
  <DockPanel>
    <TabControl>
      <TabItem Header="ğŸ“‹ Stok Hareketleri">
        <ContentControl Content="{Binding DocumentsView}"/>
      </TabItem>
      <TabItem Header="ğŸ“¦ Stok KartlarÄ±">
        <ContentControl Content="{Binding StocksView}"/>
      </TabItem>
      <TabItem Header="ğŸ¢ Depo YÃ¶netimi">
        <!-- R-040: Warehouse Management using existing WarehouseCardsView (R-034) -->
        <ContentControl Content="{Binding WarehouseManagementView}"/>
      </TabItem>
    </TabControl>
  </DockPanel>
</UserControl>
